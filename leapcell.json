{
  "$schema": "https://leapcell.io/schema.json",
  "build": {
    "builder": "paketobuildpacks/builder:base"
  },
  "run": [
    {
      "command": "uvicorn app:app --host 0.0.0.0 --port $PORT",
      "networkMode": "host"
    }
  ]
}
